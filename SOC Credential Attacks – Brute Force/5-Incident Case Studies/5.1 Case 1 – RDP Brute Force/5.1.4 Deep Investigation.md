### **1️. Failed → Success Analysis**

1. **Failed logon attempts** (Event ID 4625) show the attacker repeatedly tried credentials:  

   - Logon Type: **3 (Network)** → RDP connection  
   - Source IP: `192.168.1.106`  
   - Target User: `testuser`  
   - Status: `0xC000006D / 0xC000006A` → bad username/password  

2. **Successful login** (Event ID 4624) indicates the attacker guessed the correct password:

   - Logon Type: **3 (Network)**  
   - Target User: `testuser`  
   - Source IP: `192.168.1.106`  

**Observation:** The sequence of multiple failed logins followed by a successful login confirms the brute force attack.

---

### **2️. Logon Type Analysis**

| Logon Type | Description                        | Evidence in Attack |
| --------- | ---------------------------------- | ----------------- |
| 2         | Interactive (local console)        | Not used in attack |
| 3         | Network (remote, RDP/SMB)          | Used by attacker  |
| 5         | Service                            | Seen for system processes |
| 10        | RemoteInteractive (RDP with NLA)   | Not triggered     |

> RDP brute force attacks primarily use **Logon Type 3** (Network), consistent with the events.

---

### **3️. Sysmon Post-Login Activity**

1. **Process creation (Event ID 1):**  
   - `splunk-powershell.exe` triggered on login  
   - Confirms attacker ran scripts after successful access

2. **Network connections (Event ID 3):**  
   - TCP connections from attacker IP `192.168.1.106`  
   - Target: RDP port 3389 on `192.168.1.107`  

3. **Credential access (Event ID 10):**  
   - Target process: `lsass.exe`  
   - Shows post-login attempt to access credentials  

**Insight:** Sysmon logs confirm **post-login malicious activity**, validating the attack and potential credential harvesting attempt.

---

### **4️. Investigation Summary**

- Multiple failed logons → successful login → post-login activity  
- Target user: `testuser`  
- Attacker IP: `192.168.1.106`  
- Logon Type: 3 (Network / RDP)  
- Sysmon events confirm script execution and credential access  
